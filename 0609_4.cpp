#include <bits/stdc++.h>
using namespace std;

int main() {
    string a, b, c;
    cin >> a >> b >> c;
    vector<string> v{a, b, c};
    for (int i = 0; i < v.size(); i++) {
        cout << v[i] << endl;
    }
}