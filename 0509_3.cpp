#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if (a >= c) {
        cout << a + b << endl;

    }
    else {
        cout << a << endl;
    }
}