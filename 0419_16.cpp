#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m, l;
    cin >> n >> m >> l;

    if (n >= 20 && m >= 20 && l >= 20) {
        cout << "OK" << endl;
    }
    else {
        cout << "NG" << endl;
    }

    }
