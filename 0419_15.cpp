#include <bits/stdc++.h>
using namespace std;

int main() {
    string s, m;
    cin >> s >> m;
    if (s.size() == m.size()) {
        cout << "Yes" << endl;
    }
    else {
        cout << "No" << endl;
    }
}