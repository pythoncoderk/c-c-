#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    vector<int> v;
    int i = 0;
    while (true) {
        if (b == 24) {
            b = 0;

        }
        v.push_back(b);
        b++;
        if (b == c) {
            break;
        }


    }
}