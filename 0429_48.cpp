#include <bits/stdc++.h>
using namespace std;

int main() {
    int s;
    string m;
    cin >> s >> m;
    int count1 = 0;
    for (int i = 0; i < m.size(); i++) {
        if (m[i] == (char)s) {

        }

    }
}