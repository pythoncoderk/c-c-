#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m, x;
    cin >> n >> m >> x;
    if (n >= x) {
        cout << x + m << endl;
    }
    else {
        cout << x << endl;
    }
}