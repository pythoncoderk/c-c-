#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    vector<string> v(n+2, vector<string>(m+2, "#"));
    for (int i = 0; i < n+2; i++) {
        for (int j = 0; j < m+2; j++) {

        }
    }
}